[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "nodejs"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "python3 backend/app.py"
